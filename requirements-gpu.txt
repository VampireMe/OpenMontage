# OpenMontage - GPU Dependencies
# Install these in addition to requirements.txt when GPU is available
torch>=2.0
torchaudio>=2.0
torchvision>=0.15
mlx-audio>=0.4.4; platform_system == "Darwin" and platform_machine == "arm64"
huggingface_hub>=1.20.1
